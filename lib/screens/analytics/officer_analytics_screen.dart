import 'package:flutter/material.dart';

class OfficerAnalyticsScreen extends StatelessWidget {
  const OfficerAnalyticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OfficerAnalyticsScreen')),
      body: Center(child: const Text('OfficerAnalyticsScreen')),
    );
  }
}
