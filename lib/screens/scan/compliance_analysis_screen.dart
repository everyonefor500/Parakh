import 'package:flutter/material.dart';

class ComplianceAnalysisScreen extends StatelessWidget {
  const ComplianceAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ComplianceAnalysisScreen')),
      body: Center(child: const Text('ComplianceAnalysisScreen')),
    );
  }
}
